{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "platform-server.serviceAccountName" . }}
  labels:
    {{- include "platform-server.labels" . | nindent 4 }}
{{- end }}